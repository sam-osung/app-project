apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: frontend-tls
  namespace: default
spec:
  secretName: frontend-tls-secret
  dnsNames:
    - samone.store
    - api.samone.store
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer